% DOCUMENT SETUP
\onehalfspacing
\setlength		{\parindent}{0cm} % Default is 15pt.
\setlength		{\parskip}{0.5em}
\widowpenalty10000
\clubpenalty10000

% ToC SETUP
\cftsetindents	{section}{0em}{4em}
\cftsetindents	{subsection}{0em}{4em}
\cftsetindents	{subsubsection}{0em}{4em}
\renewcommand	{\contentsname}{Inhaltsverzeichnis}
\setcounter		{tocdepth}{3}
\setcounter		{secnumdepth}{5}

% ABBILDUNGEN UND TABELLEN SETUP 
\renewcommand	{\listfigurename}{Abbildungsverzeichnis}
\renewcommand	{\listtablename}{Tabellenverzeichnis}

\addto{\captionsngerman}{%
    \renewcommand*{\figurename}{Abb.}
    \renewcommand*{\tablename}{Tab.}
}

\addtocontents{lof}{\linespread{2}\selectfont}
\addtocontents{lot}{\linespread{2}\selectfont}

\makeatletter
\renewcommand{\cftfigpresnum}{Abb. }
\renewcommand{\cfttabpresnum}{Tab. }

\setlength{\cftfignumwidth}{2cm}
\setlength{\cfttabnumwidth}{2cm}

\setlength{\cftfigindent}{0cm}
\setlength{\cfttabindent}{0cm}
\makeatother

% CAPTION SETUP
\captionsetup{%
    font=small,
    labelfont=bf,
    singlelinecheck=false,
    skip=10pt,
    belowskip=0pt
}

\renewcommand*{\labelalphaothers}{\textsuperscript{}}

% HEADERS & FOOTERS
\pagestyle		{fancyplain}
\fancyfoot		{}
\fancyhf		{}
\renewcommand	{\headrulewidth}{0pt}
\renewcommand	{\footrulewidth}{0pt}
\fancyhead		[c]{\thepage}
\setlength		{\headheight}{15pt}

% PARAGRAPH SETUP
\newcommand{\dhgeparagraph}[1]{\paragraph{#1}\mbox{}\\\vspace{-1.5em}}

% FOOTNOTE
\renewcommand{\footnotelayout}{\hspace{0.5em}}

% AUTISTIC CONDITIONALS
% This hides ToF and ToT if they are empty
\makeatletter
\AtEndEnvironment	{figure}{\gdef\there@is@a@figure{}}
\AtEndEnvironment	{table}{\gdef\there@is@a@table{}}

\AtEndDocument		{\ifdefined\there@is@a@figure\label{fig:was:used:in:doc}\fi}
\AtEndDocument		{\ifdefined\there@is@a@table\label{tab:was:used:in:doc}\fi}
\newcommand{\conditionalLoF}{\@ifundefined{r@fig:was:used:in:doc}{}
    {\addcontentsline{toc}{section}{\listfigurename} \listoffigures \cleardoublepage}}%
\newcommand{\conditionalLoT}{\@ifundefined{r@tab:was:used:in:doc}{}
    {\addcontentsline{toc}{section}{\listtablename} \listoftables \cleardoublepage}}%
\makeatother

% COUNTER (So we can keep counting in Roman after switching to Arabic)
\newcounter{savepage}

% new section new page
\let\stdsection\section
\renewcommand\section{\newpage\stdsection}

% override \mathrm{} -> add standard spacing
\let\oldMathrm\mathrm
\renewcommand{\mathrm}[1]{\,\oldMathrm{#1}}

% input path is the root-folder 
% only after \input{setup.tex} so all new files in the root will be integrated
\makeatletter
\def\input@path{{../}{path1/}}
\makeatother

% define the old CAUTHOR variable
\def\CAUTHOR{\CAUTHORVOR\ \CAUTHORNACH}

% Abkürzungsverzeichnis SETUP
\acsetup{
	list/heading = section*,
	list/name = {Abkürzungsverzeichnis},
	make-links = true,
	link-only-first = false
}

% Abkürzungsverzeichnis redefine of \ac
% New Counter to count used acronyms:
\newtotcounter{acro_num}			
% Normal \ac Commands:
\def\oldac{} \let\oldac=\ac
\def\ac{\stepcounter{acro_num}\oldac}

\def\oldAc{} \let\oldAc=\Ac
\def\Ac{\stepcounter{acro_num}\oldAc}

\def\oldacp{} \let\oldacp=\acp
\def\acp{\stepcounter{acro_num}\oldacp}

\def\oldAcp{} \let\oldAcp=\Acp
\def\Acp{\stepcounter{acro_num}\oldAcp}

\def\oldiac{} \let\oldiac=\iac
\def\iac{\stepcounter{acro_num}\oldiac}

\def\oldIac{} \let\oldIac=\Iac
\def\Iac{\stepcounter{acro_num}\oldIac}

% short forms

\def\oldacs{} \let\oldasc=\acs
\def\acs{\stepcounter{acro_num}\oldacs}

\def\oldAcs{} \let\oldAcs=\Acs
\def\Acs{\stepcounter{acro_num}\oldAcs}

\def\oldacsp{} \let\oldacsp=\acsp
\def\acsp{\stepcounter{acro_num}\oldacsp}

\def\oldAcsp{} \let\oldAcsp=\Acsp
\def\Acsp{\stepcounter{acro_num}\oldAcsp}

\def\oldiacs{} \let\oldiacs=\iacs
\def\iacs{\stepcounter{acro_num}\oldiacs}

\def\oldIacs{} \let\oldIacs=\Iacs
\def\Iacs{\stepcounter{acro_num}\oldIacs}

% long forms

\def\oldacl{} \let\oldacl=\acl
\def\acl{\stepcounter{acro_num}\oldacl}

\def\oldAcl{} \let\oldAcl=\Acl
\def\Acl{\stepcounter{acro_num}\oldAcl}

\def\oldaclp{} \let\oldaclp=\aclp
\def\aclp{\stepcounter{acro_num}\oldaclp}

\def\oldAclp{} \let\oldAclp=\Aclp
\def\Aclp{\stepcounter{acro_num}\oldAclp}

\def\oldiacl{} \let\oldiacl=\iacl
\def\iacl{\stepcounter{acro_num}\oldiacl}

\def\oldIacl{} \let\oldIacl=\Iacl
\def\Iacl{\stepcounter{acro_num}\oldIacl}

% alternative form

\def\oldaca{} \let\oldaca=\aca
\def\aca{\stepcounter{acro_num}\oldaca}

\def\oldAca{} \let\oldAca=\Aca
\def\Aca{\stepcounter{acro_num}\oldAca}

\def\oldacap{} \let\oldacap=\acap
\def\acap{\stepcounter{acro_num}\oldacap}

\def\oldAcap{} \let\oldAcap=\Acap
\def\Acap{\stepcounter{acro_num}\oldAcap}

\def\oldiaca{} \let\oldiaca=\iaca
\def\iaca{\stepcounter{acro_num}\oldiaca}

\def\oldIaca{} \let\oldIaca=\Iaca
\def\Iaca{\stepcounter{acro_num}\oldIaca}

% full form

\def\oldacf{} \let\oldacf=\acf
\def\acf{\stepcounter{acro_num}\oldacf}

\def\oldAcf{} \let\oldAcf=\Acf
\def\Acf{\stepcounter{acro_num}\oldAcf}

\def\oldacfp{} \let\oldacfp=\acfp
\def\acfp{\stepcounter{acro_num}\oldacfp}

\def\oldAcfp{} \let\oldAcfp=\Acfp
\def\Acfp{\stepcounter{acro_num}\oldAcfp}

\def\oldiacf{} \let\oldiacf=\iacf
\def\iacf{\stepcounter{acro_num}\oldiacf}

\def\oldIacf{} \let\oldIacf=\Iacf
\def\Iacf{\stepcounter{acro_num}\oldIacf}
